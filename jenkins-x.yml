buildPack: gradle
pipelineConfig:
  agent:
    image: gcr.io/jenkinsxio/builder-gradle5-java11:latest
  pipelines:
    pullRequest:
      preBuild:
        steps:
          - name: check-step-execution
            command: echo "IT'S WORKING!"
